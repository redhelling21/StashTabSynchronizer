{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "${command:python.interpreterPath}",
      "args": ["scripts/build.py"],
      "isBackground": true,
      "options": {"cwd": "${workspaceFolder}"},
      "label": "Build",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}